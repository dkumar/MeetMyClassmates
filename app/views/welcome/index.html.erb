<h1>Calendar</h1>
<div>
  <div class="calendar"></div>
</div>
<div id = "create_event_dialog" class="dialog" style ="display:none;"></div>
<div id = "event_desc_dialog" class="dialog" style ="display:none;"></div>

<script type = 'text/javascript'>
    //full_calendar_options.Remove("slotMinutes");
    //full_calendar_options.Add("SlotMinutes", 60);
    calendar_options = {
        defaultView: "agendaWeek",
        dragOpacity: 0.5,
        editable: true,
        slotMinutes: 60,
        timeFormat: "h:mm t{ - h:mm t}",
        height: 600,
        header: {
            center: "title",
            left: "prev,next today",
            right: "month,agendaWeek,agendaDay"
        },
        selectable: true,
        minTime: "08:00:00",
        maxTime: "24:00:00",
        allDaySlot: false,
    }
    $('.calendar').fullCalendar(calendar_options);

    console.log(full_calendar_options);

</script>